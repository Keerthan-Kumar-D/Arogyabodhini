# ZEGOCLOUD Credentials (Frontend — public values only)
# The Server Secret is NEVER exposed here. It stays in server/.env only.

# App ID (number from ZEGOCLOUD Console)
VITE_ZEGO_APP_ID=1440376687

# Backend API URL (the Express server that generates secure tokens)
VITE_API_BASE_URL=https://arogyabodhini.onrender.com
